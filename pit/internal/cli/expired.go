package cli

const ExpiredCopy = "Your local session expired. Create a secure PIT session again. If extraAgents still lists this agent, PIT reuses it and does not mint a new address."
const RevokedCopy = "Your session was revoked. Order and cancel are blocked."
