export function TransferNote() {
  return (
    <p className="mt-4 max-w-[48ch] text-sm opacity-80">
      Desk ID usage is authorized on Aristotle by the owner wallet. The session key cannot mint or move identity.
    </p>
  );
}
