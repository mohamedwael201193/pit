export function RecoverNote() {
  return (
    <p className="fine">
      Refresh recovers the exact preview on this machine. The browser cannot sign. Duplicate clicks do not post twice.
    </p>
  );
}
