export function CancelNote() {
  return (
    <p className="fine">
      Cancel queries open orders on the matching venue first. It cannot withdraw. A linked agent can post that cancel only when that order is still on the venue.
    </p>
  );
}
