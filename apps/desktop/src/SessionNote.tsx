export function SessionNote() {
  return (
    <p className="fine">
      Create the 24-hour session on this machine. If Hyperliquid still lists this PIT agent, PIT reuses it. Your wallet approves the agent. PIT never prints the key.
    </p>
  );
}
