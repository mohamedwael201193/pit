export function LinkedNote() {
  return (
    <p className="fine">
      PIT checks Hyperliquid approval on the matching venue. A missing or expired agent cannot post. A linked agent can post the bound order only after AUTHORIZE.
    </p>
  );
}
