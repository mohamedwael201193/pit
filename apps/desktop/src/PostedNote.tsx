export function PostedNote() {
  return (
    <p className="fine">
      After AUTHORIZE, PIT posts only a signed order or cancel whose session agent is listed on extraAgents. Unsigned, unlinked, or hash-mismatched payloads never reach the venue. MCP and the browser never post.
    </p>
  );
}
