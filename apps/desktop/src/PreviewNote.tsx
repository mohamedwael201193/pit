export function PreviewNote() {
  return (
    <p className="fine">
      The exact preview binds market, side, size, and session. Any mutation invalidates authorization. PIT does not send an order from the browser.
    </p>
  );
}
