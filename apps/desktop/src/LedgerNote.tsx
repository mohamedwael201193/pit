export function LedgerNote() {
  return (
    <p className="fine">
      AUTHORIZE records the exact preview on this machine. An unsigned payload never reaches the venue. Duplicate clicks do not post twice.
    </p>
  );
}
