export function SignedNote() {
  return (
    <p className="fine">
      A local L1 signature is not a venue fill. PIT does not post until the trading account has approved the session agent.
    </p>
  );
}
